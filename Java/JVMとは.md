## JVM
プログラミング言語は、異なるOSやCPUを動かすことは出来ません。動かすためには、それぞれのOS、CPUに合わせた機械語に変換する必要があるためです。(Wndows専用の機能だったり、Apple社のCPUに合わせた仕様など)<br>
Javaは同じソースコードで異なるOSやCPUで動かすことが出来ます。それを可能にするのが**JVM**(JavaVirualMachine)です。<br>

Javaはまず**Javaコンパイラ**を使用し、Javaバイトコードにコンパイル(変換)します。

### git reflog
gitの操作履歴を確認できる。
### git reset --hard HEAD@{n}
reflogでチェックした作業番号を指定すると、該当の作業場所まで戻すことができる。（--hardはワーキングツリーまで全ての変更点を元に戻す）
### git stash push -m "コメント" -- ファイル名 ファイル名1
コメントを打つ場合は-mオプションを使用する。全てのファイルだけでなく個別のファイルを退避したい場合はスペース区切りでファイル名を指定する。
